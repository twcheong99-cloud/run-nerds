{
  "type": "module",
  "scripts": {
    "test": "node tests/coach-service.test.mjs",
    "mobile:prepare": "node scripts/prepare-capacitor-web.mjs",
    "mobile:sync": "npm run mobile:prepare && npx cap sync",
    "mobile:doctor": "npx cap doctor",
    "mobile:open:android": "npm run mobile:prepare && npx cap open android",
    "mobile:open:ios": "npm run mobile:prepare && npx cap open ios",
    "native:doctor": "node scripts/native-release-doctor.mjs",
    "release:check": "node scripts/release-check.mjs"
  },
  "devDependencies": {
    "@capacitor/android": "^8.3.4",
    "@capacitor/cli": "^8.3.4",
    "@capacitor/core": "^8.3.4",
    "@capacitor/ios": "^8.3.4"
  }
}
